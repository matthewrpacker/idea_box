<div class="container-fluid">
  <div class="row">
    <div class="col-md-offset-2">
      <table>
        <thead class="idea-head">
          <h1 class="table-title">All Ideas</h1>
          <tr>
            <th>Title</th>
            <th id="th-body">Body</th>
            <th>Quality</th>
          </tr>
        </thead>
        <tbody class="idea-body">
          <% @ideas.each do |idea| %>
          <tr class="idea" data-target="<%= idea.id %>">
            <td><%= idea.title %></td>
            <td id="td-body"><%= idea.body.truncate(100, separator: ' ') %></td>
            <td><%= idea.rating %></td>
            <td><button type="button" name="button" class="btn btn-danger btn-xs">Delete</button></td>
          </tr>
          <% end %>
        </tbody>
      </table><br>
    </div>
  </div>
  <div class="row">
    <div class="col-md-offset-2">
      <form class="idea-form" method="post" action="/api/v1/ideas">
        <input id="idea-title" type="text" placeholder="Enter new idea title" /><br>
        <input id="idea-body" type="text" placeholder="Enter new idea description" /><br>
        <input type="submit" value="Save" class="btn btn-success" />
      </form>
    </div>
  </div>
</div>
